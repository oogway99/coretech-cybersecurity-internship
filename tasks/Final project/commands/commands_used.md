#	Attacker’s info:
Use commands (hostname, hostname –I, cat /etc/os-release)
# Target #1 info (Ubuntu vm)
Use commands (hostname, hostname –I, cat /etc/os-release)
# Target #2 info (metasploitable 2)
Use commands (hostname, ifconfig, uname -a)
